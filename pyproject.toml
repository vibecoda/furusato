[project]
name = "furusato-listings"
version = "0.1.0"
description = "Data pipeline for Furusato Listings"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32,<3",
]

[tool.uv]
package = false
